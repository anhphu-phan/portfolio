import { Inter, Rubik, Rufina } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
});

export const rubik = Rubik({
  subsets: ["latin"],
});

export const rufina = Rufina({
  subsets: ["latin"],
  weight: ["400", "700"],
});
